// Stub — full PerStepSpeakerAssignment to be ported next pass.
export const PerStepSpeakerAssignment = ({ steps }: { steps: any[]; setSteps: any }) => {
  return (
    <div className="space-y-3">
      <h3 className="text-sm font-heading font-semibold">Assign a Speaker to Each Step</h3>
      <p className="text-xs text-muted-foreground">Per-step speaker UI coming in next port pass. {steps.length} active step(s).</p>
    </div>
  );
};
